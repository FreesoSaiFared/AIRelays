# Non-secret identity value is normally set in wrangler.jsonc / deployment UI.
OWNER_PRINCIPAL_ID="tid:replace-me"

# Secret: use at least 32 random bytes, e.g. `openssl rand -hex 32`.
OWNER_SETUP_TOKEN="replace-with-a-long-random-secret"
